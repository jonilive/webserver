services:
  webserver:
    build: .
    container_name: webserver
    ports:
      - "80:80"
    volumes:
      - ./appdata/config:/var/config
      - ./appdata/log:/var/log
      - ./appdata/www:/www